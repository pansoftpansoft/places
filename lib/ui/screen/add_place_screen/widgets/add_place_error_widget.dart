import 'package:flutter/material.dart';

class AddPlaceErrorWidget extends StatelessWidget {
  const AddPlaceErrorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
